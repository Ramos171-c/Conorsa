<UserControl x:Class="EnterpriseBillingSystem.Wpf.Views.Inventory.InventoryAuditsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
             xmlns:local="clr-namespace:EnterpriseBillingSystem.Wpf.Views.Inventory"
             mc:Ignorable="d" 
             d:DesignHeight="700" d:DesignWidth="1000"
             Loaded="UserControl_Loaded">

    <Grid Margin="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <!-- Header -->
        <Grid Grid.Row="0" Margin="0,0,0,16">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="Auto" />
            </Grid.ColumnDefinitions>

            <StackPanel Grid.Column="0">
                <TextBlock Text="Auditoría Comercial - Historial de Precios y Costos" Style="{StaticResource MaterialDesignHeadline4TextBlock}" FontWeight="Bold" Foreground="{DynamicResource PrimaryHueMidBrush}" />
                <TextBlock Text="Consulte la bitácora de auditoría histórica para ver cuándo, quién y por qué se cambiaron los precios o costos de un producto." Style="{StaticResource MaterialDesignBody2TextBlock}" Opacity="0.7" />
            </StackPanel>

            <Button Grid.Column="1" Command="{Binding LoadAuditLogsCommand}" Style="{StaticResource MaterialDesignFlatButton}" VerticalAlignment="Center">
                <StackPanel Orientation="Horizontal">
                    <materialDesign:PackIcon Kind="Refresh" Width="18" Height="18" Margin="0,0,8,0" VerticalAlignment="Center" />
                    <TextBlock Text="Actualizar" VerticalAlignment="Center" />
                </StackPanel>
            </Button>
        </Grid>

        <!-- Filters -->
        <materialDesign:Card Grid.Row="1" Margin="0,0,0,16" Padding="16" materialDesign:ElevationAssist.Elevation="Dp1">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*" />
                </Grid.ColumnDefinitions>

                <ComboBox Grid.Column="0" ItemsSource="{Binding Products}" 
                          SelectedValue="{Binding SelectedProductId}" 
                          SelectedValuePath="Id" 
                          DisplayMemberPath="Name" 
                          materialDesign:HintAssist.Hint="Seleccione un producto para ver el historial"
                          Style="{StaticResource MaterialDesignOutlinedComboBox}" />
            </Grid>
        </materialDesign:Card>

        <!-- Data Grid -->
        <materialDesign:Card Grid.Row="2" materialDesign:ElevationAssist.Elevation="Dp1">
            <Grid>
                <ProgressBar Style="{StaticResource MaterialDesignLinearProgressBar}" IsIndeterminate="True" 
                             Visibility="{Binding IsLoading, Converter={StaticResource BooleanToVisibilityConverter}}" 
                             VerticalAlignment="Top" Height="4" Panel.ZIndex="10" />

                <DataGrid ItemsSource="{Binding AuditLogs}" AutoGenerateColumns="False" IsReadOnly="True" HeadersVisibility="Column">
                    <DataGrid.Columns>
                        <DataGridTextColumn Header="Fecha Cambio" Binding="{Binding ChangedAt, StringFormat={}{0:dd/MM/yyyy HH:mm}}" Width="140" ElementStyle="{StaticResource MaterialDesignDataGridTextColumnStyle}" />
                        <DataGridTextColumn Header="Modificado Por" Binding="{Binding ChangedBy}" Width="120" ElementStyle="{StaticResource MaterialDesignDataGridTextColumnStyle}" />
                        
                        <!-- Cost: Old -> New -->
                        <DataGridTemplateColumn Header="Costo (Antes -> Después)" Width="150">
                            <DataGridTemplateColumn.CellTemplate>
                                <DataTemplate>
                                    <StackPanel Orientation="Horizontal" HorizontalAlignment="Left">
                                        <TextBlock Text="{Binding OldCost, StringFormat={}{0:C2}}" Opacity="0.5" />
                                        <materialDesign:PackIcon Kind="ArrowRight" Width="14" Height="14" Margin="6,0" VerticalAlignment="Center" Opacity="0.5" />
                                        <TextBlock Text="{Binding NewCost, StringFormat={}{0:C2}}" FontWeight="SemiBold" />
                                    </StackPanel>
                                </DataTemplate>
                            </DataGridTemplateColumn.CellTemplate>
                        </DataGridTemplateColumn>

                        <!-- Retail Price: Old -> New -->
                        <DataGridTemplateColumn Header="Precio Detalle (Antes -> Después)" Width="180">
                            <DataGridTemplateColumn.CellTemplate>
                                <DataTemplate>
                                    <StackPanel Orientation="Horizontal" HorizontalAlignment="Left">
                                        <TextBlock Text="{Binding OldRetailPrice, StringFormat={}{0:C2}}" Opacity="0.5" />
                                        <materialDesign:PackIcon Kind="ArrowRight" Width="14" Height="14" Margin="6,0" VerticalAlignment="Center" Opacity="0.5" />
                                        <TextBlock Text="{Binding NewRetailPrice, StringFormat={}{0:C2}}" FontWeight="SemiBold" Foreground="#1E88E5" />
                                    </StackPanel>
                                </DataTemplate>
                            </DataGridTemplateColumn.CellTemplate>
                        </DataGridTemplateColumn>

                        <!-- SemiWholesale Price: Old -> New -->
                        <DataGridTemplateColumn Header="Precio Semi Mayorista (Antes -> Después)" Width="200">
                            <DataGridTemplateColumn.CellTemplate>
                                <DataTemplate>
                                    <StackPanel Orientation="Horizontal" HorizontalAlignment="Left">
                                        <TextBlock Text="{Binding OldSemiWholesalePrice, StringFormat={}{0:C2}}" Opacity="0.5" />
                                        <materialDesign:PackIcon Kind="ArrowRight" Width="14" Height="14" Margin="6,0" VerticalAlignment="Center" Opacity="0.5" />
                                        <TextBlock Text="{Binding NewSemiWholesalePrice, StringFormat={}{0:C2}}" FontWeight="SemiBold" Foreground="#43A047" />
                                    </StackPanel>
                                </DataTemplate>
                            </DataGridTemplateColumn.CellTemplate>
                        </DataGridTemplateColumn>

                        <!-- Wholesale Price: Old -> New -->
                        <DataGridTemplateColumn Header="Precio Mayorista (Antes -> Después)" Width="190">
                            <DataGridTemplateColumn.CellTemplate>
                                <DataTemplate>
                                    <StackPanel Orientation="Horizontal" HorizontalAlignment="Left">
                                        <TextBlock Text="{Binding OldWholesalePrice, StringFormat={}{0:C2}}" Opacity="0.5" />
                                        <materialDesign:PackIcon Kind="ArrowRight" Width="14" Height="14" Margin="6,0" VerticalAlignment="Center" Opacity="0.5" />
                                        <TextBlock Text="{Binding NewWholesalePrice, StringFormat={}{0:C2}}" FontWeight="SemiBold" Foreground="#E65100" />
                                    </StackPanel>
                                </DataTemplate>
                            </DataGridTemplateColumn.CellTemplate>
                        </DataGridTemplateColumn>

                        <DataGridTextColumn Header="Motivo / Razón del Cambio" Binding="{Binding Reason}" Width="*" ElementStyle="{StaticResource MaterialDesignDataGridTextColumnStyle}" />
                    </DataGrid.Columns>
                </DataGrid>
            </Grid>
        </materialDesign:Card>
    </Grid>
</UserControl>
