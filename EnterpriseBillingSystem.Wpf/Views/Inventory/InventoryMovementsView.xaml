<UserControl x:Class="EnterpriseBillingSystem.Wpf.Views.Inventory.InventoryMovementsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
             xmlns:local="clr-namespace:EnterpriseBillingSystem.Wpf.Views.Inventory"
             mc:Ignorable="d" 
             d:DesignHeight="700" d:DesignWidth="1000"
             Loaded="UserControl_Loaded">

    <Grid Margin="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <!-- Header -->
        <Grid Grid.Row="0" Margin="0,0,0,16">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="Auto" />
            </Grid.ColumnDefinitions>

            <StackPanel Grid.Column="0">
                <TextBlock Text="Kardex - Movimientos de Inventario" Style="{StaticResource MaterialDesignHeadline4TextBlock}" FontWeight="Bold" Foreground="{DynamicResource PrimaryHueMidBrush}" />
                <TextBlock Text="Consulte la bitácora histórica detallada de entradas y salidas de un producto por bodega." Style="{StaticResource MaterialDesignBody2TextBlock}" Opacity="0.7" />
            </StackPanel>

            <Button Grid.Column="1" Command="{Binding LoadMovementsCommand}" Style="{StaticResource MaterialDesignFlatButton}" VerticalAlignment="Center">
                <StackPanel Orientation="Horizontal">
                    <materialDesign:PackIcon Kind="Refresh" Width="18" Height="18" Margin="0,0,8,0" VerticalAlignment="Center" />
                    <TextBlock Text="Actualizar" VerticalAlignment="Center" />
                </StackPanel>
            </Button>
        </Grid>

        <!-- Filters -->
        <materialDesign:Card Grid.Row="1" Margin="0,0,0,16" Padding="16" materialDesign:ElevationAssist.Elevation="Dp1">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="150" />
                    <ColumnDefinition Width="150" />
                </Grid.ColumnDefinitions>

                <ComboBox Grid.Column="0" ItemsSource="{Binding Warehouses}" 
                          SelectedValue="{Binding SelectedWarehouseId}" 
                          SelectedValuePath="Id" 
                          DisplayMemberPath="Name" 
                          materialDesign:HintAssist.Hint="Bodega"
                          Style="{StaticResource MaterialDesignOutlinedComboBox}" 
                          Margin="0,0,8,0" />

                <ComboBox Grid.Column="1" ItemsSource="{Binding Products}" 
                          SelectedValue="{Binding SelectedProductId}" 
                          SelectedValuePath="Id" 
                          DisplayMemberPath="Name" 
                          materialDesign:HintAssist.Hint="Producto"
                          Style="{StaticResource MaterialDesignOutlinedComboBox}" 
                          Margin="8,0,8,0" />

                <DatePicker Grid.Column="2" SelectedDate="{Binding StartDate}" 
                            materialDesign:HintAssist.Hint="Fecha Desde"
                            Style="{StaticResource MaterialDesignOutlinedDatePicker}" 
                            Margin="8,0,8,0" />

                <DatePicker Grid.Column="3" SelectedDate="{Binding EndDate}" 
                            materialDesign:HintAssist.Hint="Fecha Hasta"
                            Style="{StaticResource MaterialDesignOutlinedDatePicker}" 
                            Margin="8,0,0,0" />
            </Grid>
        </materialDesign:Card>

        <!-- Data Grid -->
        <materialDesign:Card Grid.Row="2" materialDesign:ElevationAssist.Elevation="Dp1">
            <Grid>
                <ProgressBar Style="{StaticResource MaterialDesignLinearProgressBar}" IsIndeterminate="True" 
                             Visibility="{Binding IsLoading, Converter={StaticResource BooleanToVisibilityConverter}}" 
                             VerticalAlignment="Top" Height="4" Panel.ZIndex="10" />

                <DataGrid ItemsSource="{Binding Movements}" AutoGenerateColumns="False" IsReadOnly="True" HeadersVisibility="Column">
                    <DataGrid.Columns>
                        <DataGridTextColumn Header="Fecha" Binding="{Binding MovementDate, StringFormat={}{0:dd/MM/yyyy HH:mm}}" Width="140" ElementStyle="{StaticResource MaterialDesignDataGridTextColumnStyle}" />
                        <DataGridTextColumn Header="Nro. Movimiento" Binding="{Binding MovementNumber}" Width="120" ElementStyle="{StaticResource MaterialDesignDataGridTextColumnStyle}" />
                        <DataGridTextColumn Header="Tipo" Binding="{Binding MovementTypeName}" Width="150" ElementStyle="{StaticResource MaterialDesignDataGridTextColumnStyle}" />
                        <DataGridTextColumn Header="Documento Ref." Binding="{Binding ReferenceDocument}" Width="120" ElementStyle="{StaticResource MaterialDesignDataGridTextColumnStyle}" />
                        
                        <!-- Flow indicator / Sign -->
                        <DataGridTemplateColumn Header="Sentido" Width="100">
                            <DataGridTemplateColumn.CellTemplate>
                                <DataTemplate>
                                    <Border CornerRadius="4" Padding="6,2" HorizontalAlignment="Center">
                                        <Border.Style>
                                            <Style TargetType="Border">
                                                <Style.Triggers>
                                                    <DataTrigger Binding="{Binding IsEntry}" Value="True">
                                                        <Setter Property="Background" Value="#E8F5E9" />
                                                    </DataTrigger>
                                                    <DataTrigger Binding="{Binding IsEntry}" Value="False">
                                                        <Setter Property="Background" Value="#FFEBEE" />
                                                    </DataTrigger>
                                                </Style.Triggers>
                                            </Style>
                                        </Border.Style>
                                        <TextBlock FontWeight="Bold" FontSize="11">
                                            <TextBlock.Style>
                                                <Style TargetType="TextBlock">
                                                    <Style.Triggers>
                                                        <DataTrigger Binding="{Binding IsEntry}" Value="True">
                                                            <Setter Property="Text" Value="ENTRADA" />
                                                            <Setter Property="Foreground" Value="#2E7D32" />
                                                        </DataTrigger>
                                                        <DataTrigger Binding="{Binding IsEntry}" Value="False">
                                                            <Setter Property="Text" Value="SALIDA" />
                                                            <Setter Property="Foreground" Value="#C62828" />
                                                        </DataTrigger>
                                                    </Style.Triggers>
                                                </Style>
                                            </TextBlock.Style>
                                        </TextBlock>
                                    </Border>
                                </DataTemplate>
                            </DataGridTemplateColumn.CellTemplate>
                        </DataGridTemplateColumn>

                        <DataGridTextColumn Header="Cantidad" Binding="{Binding Quantity, StringFormat={}{0:N2}}" Width="100" ElementStyle="{StaticResource RightAlignedColumnStyle}" />
                        <DataGridTextColumn Header="Presentación" Binding="{Binding UnitOfMeasureCode}" Width="100" ElementStyle="{StaticResource MaterialDesignDataGridTextColumnStyle}" />
                        <DataGridTextColumn Header="Cant. Base" Binding="{Binding QuantityInBaseUnit, StringFormat={}{0:N2}}" Width="100" ElementStyle="{StaticResource RightAlignedColumnStyle}" />
                        <DataGridTextColumn Header="Notas" Binding="{Binding Notes}" Width="*" ElementStyle="{StaticResource MaterialDesignDataGridTextColumnStyle}" />
                    </DataGrid.Columns>
                </DataGrid>
            </Grid>
        </materialDesign:Card>

        <!-- Pagination -->
        <StackPanel Grid.Row="3" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,16,0,0">
            <TextBlock Text="Página: " VerticalAlignment="Center" Margin="0,0,8,0" />
            <TextBlock Text="{Binding PageNumber}" FontWeight="Bold" VerticalAlignment="Center" Margin="0,0,16,0" />

            <Button Command="{Binding PreviousPageCommand}" Style="{StaticResource MaterialDesignIconButton}" Width="32" Height="32" Margin="0,0,4,0">
                <materialDesign:PackIcon Kind="ChevronLeft" />
            </Button>

            <Button Command="{Binding NextPageCommand}" Style="{StaticResource MaterialDesignIconButton}" Width="32" Height="32">
                <materialDesign:PackIcon Kind="ChevronRight" />
            </Button>
        </StackPanel>
    </Grid>
</UserControl>
