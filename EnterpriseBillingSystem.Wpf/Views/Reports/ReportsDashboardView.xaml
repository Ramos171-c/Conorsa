<UserControl x:Class="EnterpriseBillingSystem.Wpf.Views.Reports.ReportsDashboardView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
             mc:Ignorable="d" 
             d:DesignHeight="700" d:DesignWidth="1100" Background="#F8FAFC">
    <Grid Margin="24">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <!-- Header -->
        <Grid Grid.Row="0" Margin="0,0,0,20">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="Auto" />
            </Grid.ColumnDefinitions>

            <StackPanel Grid.Column="0">
                <TextBlock Text="📊 Analytics &amp; Centro de Reportes" Style="{StaticResource MaterialDesignHeadline4TextBlock}" FontWeight="Bold" Foreground="#0F172A" />
                <TextBlock Text="Monitoree métricas de preventa, entrega efectiva, faltantes y exporte reportes auditados en PDF." Style="{StaticResource MaterialDesignBody2TextBlock}" Foreground="#64748B" />
            </StackPanel>

            <StackPanel Grid.Column="1" Orientation="Horizontal" VerticalAlignment="Center">
                <DatePicker SelectedDate="{Binding FromDate}" Width="120" Margin="0,0,12,0" materialDesign:HintAssist.Hint="Fecha Inicio" Style="{StaticResource MaterialDesignFloatingHintDatePicker}" />
                <DatePicker SelectedDate="{Binding ToDate}" Width="120" Margin="0,0,16,0" materialDesign:HintAssist.Hint="Fecha Fin" Style="{StaticResource MaterialDesignFloatingHintDatePicker}" />
                <Button Command="{Binding RefreshDashboardCommand}" Style="{StaticResource MaterialDesignRaisedButton}" Background="#4F46E5" BorderBrush="#4F46E5">
                    <StackPanel Orientation="Horizontal">
                        <materialDesign:PackIcon Kind="Refresh" Width="18" Height="18" Margin="0,0,8,0" VerticalAlignment="Center" />
                        <TextBlock Text="Actualizar Datos" VerticalAlignment="Center" FontWeight="Bold" />
                    </StackPanel>
                </Button>
            </StackPanel>
        </Grid>

        <!-- KPI Cards WrapPanel -->
        <WrapPanel Grid.Row="1" Margin="0,0,0,16" HorizontalAlignment="Left">
            <!-- Card 1: Preventa -->
            <materialDesign:Card Width="240" Height="125" UniformCornerRadius="12" Background="White" materialDesign:ElevationAssist.Elevation="Dp2" Padding="20" Margin="0,0,16,16">
                <StackPanel>
                    <Grid Margin="0,0,0,12">
                        <TextBlock Text="Preventa Solicitada" FontWeight="Bold" Foreground="#64748B" FontSize="13" VerticalAlignment="Center" />
                        <Border HorizontalAlignment="Right" Background="#E0F2FE" CornerRadius="8" Padding="8">
                            <materialDesign:PackIcon Kind="CurrencyUsd" Foreground="#0284C7" Width="20" Height="20" />
                        </Border>
                    </Grid>
                    <TextBlock Text="{Binding TotalPresaleValue}" FontSize="22" FontWeight="Bold" Foreground="#0F172A" Margin="0,0,0,4" />
                    <TextBlock Text="{Binding TotalPresaleSub}" FontSize="12" Foreground="#64748B" />
                </StackPanel>
            </materialDesign:Card>

            <!-- Card 2: Entrega Efectiva -->
            <materialDesign:Card Width="240" Height="125" UniformCornerRadius="12" Background="White" materialDesign:ElevationAssist.Elevation="Dp2" Padding="20" Margin="0,0,16,16">
                <StackPanel>
                    <Grid Margin="0,0,0,12">
                        <TextBlock Text="Entrega Efectiva Real" FontWeight="Bold" Foreground="#64748B" FontSize="13" VerticalAlignment="Center" />
                        <Border HorizontalAlignment="Right" Background="#D1FAE5" CornerRadius="8" Padding="8">
                            <materialDesign:PackIcon Kind="TruckCheck" Foreground="#059669" Width="20" Height="20" />
                        </Border>
                    </Grid>
                    <TextBlock Text="{Binding TotalDeliveredValue}" FontSize="22" FontWeight="Bold" Foreground="#059669" Margin="0,0,0,4" />
                    <TextBlock Text="{Binding TotalDeliveredSub}" FontSize="12" Foreground="#64748B" />
                </StackPanel>
            </materialDesign:Card>

            <!-- Card 3: Pérdida por Faltantes -->
            <materialDesign:Card Width="240" Height="125" UniformCornerRadius="12" Background="White" materialDesign:ElevationAssist.Elevation="Dp2" Padding="20" Margin="0,0,16,16">
                <StackPanel>
                    <Grid Margin="0,0,0,12">
                        <TextBlock Text="Pérdida por Devoluciones" FontWeight="Bold" Foreground="#64748B" FontSize="13" VerticalAlignment="Center" />
                        <Border HorizontalAlignment="Right" Background="#FEE2E2" CornerRadius="8" Padding="8">
                            <materialDesign:PackIcon Kind="AlertCircleOutline" Foreground="#DC2626" Width="20" Height="20" />
                        </Border>
                    </Grid>
                    <TextBlock Text="{Binding TotalLossValue}" FontSize="22" FontWeight="Bold" Foreground="#DC2626" Margin="0,0,0,4" />
                    <TextBlock Text="{Binding TotalLossSub}" FontSize="12" Foreground="#64748B" />
                </StackPanel>
            </materialDesign:Card>

            <!-- Card 4: Efectividad -->
            <materialDesign:Card Width="240" Height="125" UniformCornerRadius="12" Background="White" materialDesign:ElevationAssist.Elevation="Dp2" Padding="20" Margin="0,0,0,16">
                <StackPanel>
                    <Grid Margin="0,0,0,12">
                        <TextBlock Text="Efectividad Global" FontWeight="Bold" Foreground="#64748B" FontSize="13" VerticalAlignment="Center" />
                        <Border HorizontalAlignment="Right" Background="#F3E8FF" CornerRadius="8" Padding="8">
                            <materialDesign:PackIcon Kind="ChartLine" Foreground="#7C3AED" Width="20" Height="20" />
                        </Border>
                    </Grid>
                    <TextBlock Text="{Binding EffectivenessValue}" FontSize="22" FontWeight="Bold" Foreground="#7C3AED" Margin="0,0,0,4" />
                    <TextBlock Text="{Binding EffectivenessSub}" FontSize="12" Foreground="#64748B" />
                </StackPanel>
            </materialDesign:Card>
        </WrapPanel>

        <!-- Main Content Area: PDF Download Center & Visual Quick Action Cards -->
        <Grid Grid.Row="2">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="20" />
                <ColumnDefinition Width="380" />
            </Grid.ColumnDefinitions>

            <!-- Left Panel: Center of PDF Reports (Direct Access) -->
            <materialDesign:Card Grid.Column="0" UniformCornerRadius="12" Background="White" materialDesign:ElevationAssist.Elevation="Dp2" Padding="24">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="*" />
                    </Grid.RowDefinitions>

                    <StackPanel Grid.Row="0" Margin="0,0,0,20">
                        <TextBlock Text="📄 Centro de Descarga de Reportes PDF Auditados" FontSize="18" FontWeight="Bold" Foreground="#0F172A" />
                        <TextBlock Text="Genere e imprima reportes consolidados directamente en formato PDF oficial." FontSize="13" Foreground="#64748B" Margin="0,4,0,0" />
                    </StackPanel>

                    <StackPanel Grid.Row="1" VerticalAlignment="Top">
                        <!-- Report 1: Vendedores -->
                        <Border Background="#F8FAFC" BorderBrush="#E2E8F0" BorderThickness="1" CornerRadius="10" Padding="16" Margin="0,0,0,14">
                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="Auto" />
                                    <ColumnDefinition Width="*" />
                                    <ColumnDefinition Width="Auto" />
                                </Grid.ColumnDefinitions>

                                <Border Grid.Column="0" Background="#4F46E5" CornerRadius="8" Padding="10" Margin="0,0,16,0">
                                    <materialDesign:PackIcon Kind="AccountTie" Foreground="White" Width="24" Height="24" />
                                </Border>

                                <StackPanel Grid.Column="1" VerticalAlignment="Center">
                                    <TextBlock Text="👨‍💼 Reporte de Ventas por Vendedor" FontWeight="Bold" FontSize="14" Foreground="#0F172A" />
                                    <TextBlock Text="Preventa solicitada vs entrega efectiva entregada y % efectividad por vendedor." FontSize="12" Foreground="#64748B" />
                                </StackPanel>

                                <Button Grid.Column="2" Command="{Binding OpenSellerReportPdfCommand}" Style="{StaticResource MaterialDesignRaisedButton}" Background="#4F46E5" BorderBrush="#4F46E5" VerticalAlignment="Center">
                                    <StackPanel Orientation="Horizontal">
                                        <materialDesign:PackIcon Kind="FilePdfBox" Width="18" Height="18" Margin="0,0,6,0" />
                                        <TextBlock Text="Abrir PDF" FontWeight="Bold" />
                                    </StackPanel>
                                </Button>
                            </Grid>
                        </Border>

                        <!-- Report 2: Devoluciones de Ruta -->
                        <Border Background="#F8FAFC" BorderBrush="#E2E8F0" BorderThickness="1" CornerRadius="10" Padding="16" Margin="0,0,0,14">
                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="Auto" />
                                    <ColumnDefinition Width="*" />
                                    <ColumnDefinition Width="Auto" />
                                </Grid.ColumnDefinitions>

                                <Border Grid.Column="0" Background="#0284C7" CornerRadius="8" Padding="10" Margin="0,0,16,0">
                                    <materialDesign:PackIcon Kind="TruckCheck" Foreground="White" Width="24" Height="24" />
                                </Border>

                                <StackPanel Grid.Column="1" VerticalAlignment="Center">
                                    <TextBlock Text="📋 Reporte Auditado de Faltantes y Devoluciones de Ruta" FontWeight="Bold" FontSize="14" Foreground="#0F172A" />
                                    <TextBlock Text="Detalle de productos devueltos a bodega, piezas restadas y observaciones." FontSize="12" Foreground="#64748B" />
                                </StackPanel>

                                <Button Grid.Column="2" Command="{Binding OpenReturnsReportPdfCommand}" Style="{StaticResource MaterialDesignRaisedButton}" Background="#0284C7" BorderBrush="#0284C7" VerticalAlignment="Center">
                                    <StackPanel Orientation="Horizontal">
                                        <materialDesign:PackIcon Kind="FilePdfBox" Width="18" Height="18" Margin="0,0,6,0" />
                                        <TextBlock Text="Abrir PDF" FontWeight="Bold" />
                                    </StackPanel>
                                </Button>
                            </Grid>
                        </Border>

                        <!-- Report 3: Faltantes y Pérdidas -->
                        <Border Background="#F8FAFC" BorderBrush="#E2E8F0" BorderThickness="1" CornerRadius="10" Padding="16">
                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="Auto" />
                                    <ColumnDefinition Width="*" />
                                    <ColumnDefinition Width="Auto" />
                                </Grid.ColumnDefinitions>

                                <Border Grid.Column="0" Background="#DC2626" CornerRadius="8" Padding="10" Margin="0,0,16,0">
                                    <materialDesign:PackIcon Kind="AlertOctagon" Foreground="White" Width="24" Height="24" />
                                </Border>

                                <StackPanel Grid.Column="1" VerticalAlignment="Center">
                                    <TextBlock Text="🚨 Reporte de Faltantes y Pérdidas por No Entregados" FontWeight="Bold" FontSize="14" Foreground="#0F172A" />
                                    <TextBlock Text="Monto en Córdobas (C$) no cobrado por falta de mercadería en preventa." FontSize="12" Foreground="#64748B" />
                                </StackPanel>

                                <Button Grid.Column="2" Command="{Binding OpenShortagesReportPdfCommand}" Style="{StaticResource MaterialDesignRaisedButton}" Background="#DC2626" BorderBrush="#DC2626" VerticalAlignment="Center">
                                    <StackPanel Orientation="Horizontal">
                                        <materialDesign:PackIcon Kind="FilePdfBox" Width="18" Height="18" Margin="0,0,6,0" />
                                        <TextBlock Text="Abrir PDF" FontWeight="Bold" />
                                    </StackPanel>
                                </Button>
                            </Grid>
                        </Border>
                    </StackPanel>
                </Grid>
            </materialDesign:Card>

            <!-- Right Panel: Summary Box -->
            <materialDesign:Card Grid.Column="2" UniformCornerRadius="12" Background="#1E293B" materialDesign:ElevationAssist.Elevation="Dp3" Padding="24">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="*" />
                        <RowDefinition Height="Auto" />
                    </Grid.RowDefinitions>

                    <StackPanel Grid.Row="0">
                        <TextBlock Text="📈 Resumen Ejecutivo" FontSize="18" FontWeight="Bold" Foreground="White" />
                        <TextBlock Text="{Binding DateRangeLabel}" FontSize="12" Foreground="#94A3B8" Margin="0,2,0,0" />
                    </StackPanel>

                    <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto" Margin="0,12,0,12">
                        <ItemsControl ItemsSource="{Binding RouteReturns}">
                            <ItemsControl.ItemTemplate>
                                <DataTemplate>
                                    <Border Background="#334155" CornerRadius="10" Padding="16" Margin="0,0,0,12">
                                        <StackPanel>
                                            <TextBlock Text="{Binding RouteName}" FontWeight="Bold" Foreground="#38BDF8" FontSize="14" />
                                            <TextBlock Text="{Binding ReturnedPieces, StringFormat='{}{0:N0} Piezas Devueltas Afectadas'}" Foreground="#E2E8F0" FontSize="12" Margin="0,4,0,0" />
                                            <TextBlock Text="Pedidos pasados a Completado" Foreground="#4ADE80" FontSize="12" FontWeight="Bold" Margin="0,2,0,0" />
                                        </StackPanel>
                                    </Border>
                                </DataTemplate>
                            </ItemsControl.ItemTemplate>
                        </ItemsControl>
                    </ScrollViewer>

                    <Border Grid.Row="2" Background="#0F172A" CornerRadius="8" Padding="12">
                        <TextBlock Text="✓ Sistema de Liquidaciones y Devoluciones 100% Activo" Foreground="#38BDF8" FontSize="11" FontWeight="Bold" HorizontalAlignment="Center" />
                    </Border>
                </Grid>
            </materialDesign:Card>
        </Grid>
    </Grid>
</UserControl>
