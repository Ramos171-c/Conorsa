<Window x:Class="EnterpriseBillingSystem.Wpf.Views.Dialogs.CustomInputDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
        mc:Ignorable="d"
        Title="Entrada de Datos" Height="220" Width="460"
        WindowStartupLocation="CenterOwner"
        ShowInTaskbar="False"
        ResizeMode="NoResize"
        WindowStyle="None"
        AllowsTransparency="True"
        Background="Transparent"
        FontFamily="{materialDesign:MaterialDesignFont}">

    <Border CornerRadius="12" Background="{DynamicResource MaterialDesignPaper}" BorderBrush="{DynamicResource PrimaryHueLightBrush}" BorderThickness="1.5">
        <Border.Effect>
            <DropShadowEffect BlurRadius="15" ShadowDepth="2" Direction="270" Color="#44000000" />
        </Border.Effect>
        <Grid Margin="16">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="*" />
                <RowDefinition Height="Auto" />
            </Grid.RowDefinitions>

            <!-- Header / Title Bar -->
            <Grid Grid.Row="0" Margin="0,0,0,12">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="Auto" />
                </Grid.ColumnDefinitions>
                <TextBlock x:Name="TitleTextBlock" FontSize="16" FontWeight="Bold" Foreground="{DynamicResource PrimaryHueMidBrush}" VerticalAlignment="Center" />
                <Button Grid.Column="1" Style="{StaticResource MaterialDesignFlatButton}" Padding="4" Width="28" Height="28" Click="CloseButton_Click" ToolTip="Cerrar">
                    <materialDesign:PackIcon Kind="Close" Width="20" Height="20" Foreground="{DynamicResource MaterialDesignBodyLight}" />
                </Button>
            </Grid>

            <!-- Content Area -->
            <StackPanel Grid.Row="1" Margin="0,0,0,16">
                <TextBlock x:Name="PromptTextBlock" FontSize="13.5" Margin="0,0,0,10" FontWeight="SemiBold" TextWrapping="Wrap" />
                <TextBox x:Name="InputTextBox" Style="{StaticResource MaterialDesignOutlinedTextBox}" Height="42" VerticalContentAlignment="Center" KeyDown="InputTextBox_KeyDown" />
            </StackPanel>

            <!-- Action Buttons Area -->
            <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Right">
                <Button x:Name="BtnCancel" Content="Cancelar" Style="{StaticResource MaterialDesignFlatButton}" Click="BtnCancel_Click" Margin="0,0,8,0" Width="100" Height="36" />
                <Button x:Name="BtnOk" Content="Aceptar" Style="{StaticResource MaterialDesignRaisedButton}" Click="BtnOk_Click" Width="100" Height="36" />
            </StackPanel>
        </Grid>
    </Border>
</Window>
