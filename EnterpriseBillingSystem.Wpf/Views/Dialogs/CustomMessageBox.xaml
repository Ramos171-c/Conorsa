<Window x:Class="EnterpriseBillingSystem.Wpf.Views.Dialogs.CustomMessageBox"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
        mc:Ignorable="d"
        Title="Mensaje" SizeToContent="WidthAndHeight" MaxWidth="500" MinWidth="380"
        WindowStartupLocation="CenterOwner"
        ShowInTaskbar="False"
        ResizeMode="NoResize"
        WindowStyle="None"
        AllowsTransparency="True"
        Background="Transparent"
        FontFamily="{materialDesign:MaterialDesignFont}">

    <Border CornerRadius="12" Background="{DynamicResource MaterialDesignPaper}" BorderBrush="{DynamicResource PrimaryHueLightBrush}" BorderThickness="1.5">
        <Border.Effect>
            <DropShadowEffect BlurRadius="15" ShadowDepth="2" Direction="270" Color="#44000000" />
        </Border.Effect>
        <Grid Margin="16">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="*" />
                <RowDefinition Height="Auto" />
            </Grid.RowDefinitions>

            <!-- Header / Title Bar -->
            <Grid Grid.Row="0" Margin="0,0,0,16">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="Auto" />
                </Grid.ColumnDefinitions>
                <TextBlock x:Name="TitleTextBlock" FontSize="16" FontWeight="Bold" Foreground="{DynamicResource PrimaryHueMidBrush}" VerticalAlignment="Center" />
                <Button Grid.Column="1" Style="{StaticResource MaterialDesignFlatButton}" Padding="4" Width="28" Height="28" Click="CloseButton_Click" ToolTip="Cerrar">
                    <materialDesign:PackIcon Kind="Close" Width="20" Height="20" Foreground="{DynamicResource MaterialDesignBodyLight}" />
                </Button>
            </Grid>

            <!-- Content Area (Icon + Text) -->
            <Grid Grid.Row="1" Margin="0,0,0,20">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto" />
                    <ColumnDefinition Width="*" />
                </Grid.ColumnDefinitions>

                <materialDesign:PackIcon x:Name="DialogIcon" Width="36" Height="36" Margin="0,0,16,0" VerticalAlignment="Top" />
                
                <TextBlock Grid.Column="1" x:Name="MessageTextBlock" TextWrapping="Wrap" FontSize="14.5" VerticalAlignment="Center" Foreground="{DynamicResource MaterialDesignBody}" LineHeight="20" />
            </Grid>

            <!-- Action Buttons Area -->
            <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Right">
                <!-- Yes Button -->
                <Button x:Name="BtnYes" Content="Sí" Style="{StaticResource MaterialDesignRaisedButton}" Click="BtnYes_Click" Margin="0,0,8,0" Width="90" Height="36" />
                
                <!-- No Button -->
                <Button x:Name="BtnNo" Content="No" Style="{StaticResource MaterialDesignFlatButton}" Click="BtnNo_Click" Margin="0,0,8,0" Width="90" Height="36" />
                
                <!-- OK Button -->
                <Button x:Name="BtnOk" Content="Aceptar" Style="{StaticResource MaterialDesignRaisedButton}" Click="BtnOk_Click" Margin="0,0,8,0" Width="100" Height="36" />
                
                <!-- Cancel Button -->
                <Button x:Name="BtnCancel" Content="Cancelar" Style="{StaticResource MaterialDesignFlatButton}" Click="BtnCancel_Click" Width="100" Height="36" />
            </StackPanel>
        </Grid>
    </Border>
</Window>
