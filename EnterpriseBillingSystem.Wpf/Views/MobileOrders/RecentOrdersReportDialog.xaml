<Window x:Class="EnterpriseBillingSystem.Wpf.Views.MobileOrders.RecentOrdersReportDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
        mc:Ignorable="d"
        Title="Resumen de Pedidos para Compra (Consolidado)" Height="620" Width="880"
        WindowStartupLocation="CenterOwner"
        ShowInTaskbar="False"
        ResizeMode="NoResize"
        Background="{DynamicResource MaterialDesignPaper}"
        TextElement.Foreground="{DynamicResource MaterialDesignBody}"
        TextElement.FontWeight="Medium"
        TextElement.FontSize="14"
        FontFamily="{materialDesign:MaterialDesignFont}">

    <Window.Resources>
        <BooleanToVisibilityConverter x:Key="BoolToVis" />
    </Window.Resources>

    <Grid Margin="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" /> <!-- Header -->
            <RowDefinition Height="Auto" /> <!-- Summary Info Card -->
            <RowDefinition Height="*" />    <!-- Grid -->
            <RowDefinition Height="Auto" /> <!-- Footer -->
        </Grid.RowDefinitions>

        <!-- Header -->
        <Grid Grid.Row="0" Margin="0,0,0,16">
            <StackPanel>
                <StackPanel Orientation="Horizontal" Margin="0,0,0,4">
                    <materialDesign:PackIcon Kind="ClipboardTextOutline" Width="28" Height="28" Foreground="{DynamicResource PrimaryHueMidBrush}" Margin="0,0,8,0" VerticalAlignment="Center"/>
                    <TextBlock Text="Resumen de Pedidos para Compra (Consolidado)" Style="{StaticResource MaterialDesignHeadline5TextBlock}" FontWeight="Bold" Foreground="{DynamicResource PrimaryHueMidBrush}" VerticalAlignment="Center"/>
                </StackPanel>
                <TextBlock Text="Resumen general de productos requeridos de todos los pedidos móviles en estado Recibido." Opacity="0.7" FontSize="12"/>
            </StackPanel>
        </Grid>

        <!-- Summary Info Card -->
        <materialDesign:Card Grid.Row="1" Margin="0,0,0,16" Padding="16" materialDesign:ElevationAssist.Elevation="Dp1" Background="#F8FAFC">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="Auto" />
                    <ColumnDefinition Width="32" />
                    <ColumnDefinition Width="Auto" />
                </Grid.ColumnDefinitions>

                <StackPanel Grid.Column="0" VerticalAlignment="Center">
                    <TextBlock Text="Pedidos en Estado 'Recibido'" FontWeight="Bold" FontSize="16" Foreground="{DynamicResource PrimaryHueMidBrush}" />
                    <TextBlock Text="Muestra la suma total de productos necesarios para abastecer los pedidos actuales." Opacity="0.7" FontSize="12" Margin="0,4,0,0"/>
                </StackPanel>

                <StackPanel Grid.Column="1" VerticalAlignment="Center" HorizontalAlignment="Right">
                    <TextBlock Text="CANTIDAD TOTAL" FontSize="11" Foreground="#64748B" FontWeight="Bold" HorizontalAlignment="Right" />
                    <TextBlock Text="{Binding TotalItems, StringFormat={}{0:N2}}" FontSize="20" FontWeight="Bold" Foreground="#0F172A" HorizontalAlignment="Right" />
                </StackPanel>

                <StackPanel Grid.Column="3" VerticalAlignment="Center" HorizontalAlignment="Right">
                    <TextBlock Text="VALOR ESTIMADO" FontSize="11" Foreground="#64748B" FontWeight="Bold" HorizontalAlignment="Right" />
                    <TextBlock Text="{Binding TotalEstimatedCost, StringFormat={}{0:C2}}" FontSize="20" FontWeight="Bold" Foreground="#2E7D32" HorizontalAlignment="Right" />
                </StackPanel>
            </Grid>
        </materialDesign:Card>

        <!-- Content Card -->
        <materialDesign:Card Grid.Row="2" Margin="0,0,0,16" materialDesign:ElevationAssist.Elevation="Dp1">
            <Grid>
                <ProgressBar Style="{StaticResource MaterialDesignLinearProgressBar}" IsIndeterminate="True" 
                             Visibility="{Binding IsLoading, Converter={StaticResource BoolToVis}}" 
                             VerticalAlignment="Top" Height="4" Panel.ZIndex="10" />

                <DataGrid ItemsSource="{Binding ConsolidatedProducts}" AutoGenerateColumns="False" IsReadOnly="True" HeadersVisibility="Column">
                    <DataGrid.Columns>
                        <DataGridTextColumn Header="Código" Binding="{Binding ProductCode}" Width="150" ElementStyle="{StaticResource MaterialDesignDataGridTextColumnStyle}" FontWeight="Bold" />
                        <DataGridTextColumn Header="Producto" Binding="{Binding ProductName}" Width="*" ElementStyle="{StaticResource MaterialDesignDataGridTextColumnStyle}" />
                        <DataGridTextColumn Header="U.M." Binding="{Binding UnitOfMeasure}" Width="100" ElementStyle="{StaticResource MaterialDesignDataGridTextColumnStyle}" />
                        <DataGridTextColumn Header="Cant. Total" Binding="{Binding TotalQuantity, StringFormat={}{0:N2}}" Width="130" ElementStyle="{StaticResource RightAlignedColumnStyle}" />
                        <DataGridTextColumn Header="Total Estimado" Binding="{Binding TotalCost, StringFormat={}{0:C2}}" Width="150" ElementStyle="{StaticResource RightAlignedColumnStyle}" FontWeight="Bold" />
                    </DataGrid.Columns>
                </DataGrid>
                
                <!-- Empty State Message -->
                <TextBlock Text="No se encontraron pedidos móviles en estado Recibido." 
                           HorizontalAlignment="Center" VerticalAlignment="Center"
                           Style="{StaticResource MaterialDesignBody1TextBlock}" Opacity="0.5"
                           Visibility="{Binding ShowEmptyMessage, Converter={StaticResource BoolToVis}}" />
            </Grid>
        </materialDesign:Card>

        <!-- Footer Actions -->
        <Grid Grid.Row="3">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition Width="12" />
                <ColumnDefinition Width="Auto" />
            </Grid.ColumnDefinitions>
            
            <Button Grid.Column="0" Click="ExportCsv_Click"
                    Style="{StaticResource MaterialDesignRaisedButton}" 
                    Background="#2E7D32" BorderBrush="#2E7D32"
                    IsEnabled="{Binding HasData}">
                <StackPanel Orientation="Horizontal">
                    <materialDesign:PackIcon Kind="FileExport" Width="18" Height="18" Margin="0,0,8,0" VerticalAlignment="Center" />
                    <TextBlock Text="Exportar a CSV" VerticalAlignment="Center" />
                </StackPanel>
            </Button>

            <Button Grid.Column="2" Click="ConfirmResumen_Click"
                    Style="{StaticResource MaterialDesignRaisedButton}"
                    Background="{DynamicResource PrimaryHueMidBrush}"
                    IsEnabled="{Binding HasData}">
                <StackPanel Orientation="Horizontal">
                    <materialDesign:PackIcon Kind="CheckCircleOutline" Width="18" Height="18" Margin="0,0,8,0" VerticalAlignment="Center" />
                    <TextBlock Text="Confirmar Resumen (Pasar a En Proceso)" VerticalAlignment="Center" />
                </StackPanel>
            </Button>
            
            <Button Grid.Column="4" Style="{StaticResource MaterialDesignFlatButton}" 
                    Click="Close_Click" Content="Cerrar" />
        </Grid>
    </Grid>
</Window>
