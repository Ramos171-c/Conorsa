<UserControl x:Class="EnterpriseBillingSystem.Wpf.Views.GenericModuleView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid Margin="24">
        <materialDesign:Card Padding="32" UniformCornerRadius="12">
            <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center">
                <materialDesign:PackIcon Kind="FolderOpenOutline" Width="120" Height="120" Foreground="{DynamicResource PrimaryHueMidBrush}" HorizontalAlignment="Center" Margin="0,0,0,24" />
                <TextBlock Text="{Binding Title}" FontSize="28" FontWeight="Bold" Foreground="{DynamicResource MaterialDesignBody}" HorizontalAlignment="Center" Margin="0,0,0,8" />
                <TextBlock Text="Módulo en desarrollo — Vista inicial vacía con navegación funcional." FontSize="16" Foreground="{DynamicResource MaterialDesignBodyLight}" HorizontalAlignment="Center" />
            </StackPanel>
        </materialDesign:Card>
    </Grid>
</UserControl>
